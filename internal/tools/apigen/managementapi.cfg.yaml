package: managementapi
generate:
  # Client code is generated by our custom clientgen, not oapi-codegen.
  models: true
output-options:
  type-mapping:
    string:
      formats:
        date:
          type: string
